import { Router, type Request } from 'express';
import multer from 'multer';
import { mkdirSync } from 'node:fs';
import { join } from 'node:path';
import { z } from 'zod';
import { prisma } from '../lib/prisma.js';
import { getUserId } from '../middleware/auth.js';
import { specialists } from '../services/specialists.js';

export const router = Router();
const uid = (req: Request) => getUserId(req);
const uploads = process.env.UPLOAD_DIR ?? './uploads';
mkdirSync(join(process.cwd(), uploads), { recursive: true });
const upload = multer({ dest: join(process.cwd(), uploads), limits: { fileSize: Number(process.env.MAX_UPLOAD_MB ?? 50) * 1024 * 1024 }, fileFilter: (_req, file, cb) => cb(null, ['application/pdf', 'image/jpeg', 'image/png'].includes(file.mimetype)) });
const ensureUser = (id: string) => prisma.user.upsert({ where: { id }, update: {}, create: { id } });
const profileSchema = z.object({ name: z.string().min(1).max(200), age: z.number().int().min(0).max(130), gender: z.enum(['male', 'female', 'other']), dob: z.string(), bloodGroup: z.string().max(20), location: z.string().max(300), aboutNote: z.string().max(5000), chronicConditions: z.array(z.string().max(200)), allergies: z.array(z.string().max(200)), ongoingMedications: z.array(z.unknown()), emergencyContact: z.record(z.unknown()) });
const vitalSchema = z.object({ timestamp: z.coerce.date().optional(), systolicBp: z.number().int().min(40).max(300).optional(), diastolicBp: z.number().int().min(20).max(200).optional(), glucose: z.number().min(0).optional(), glucoseContext: z.enum(['fasting','post-prandial','random']).optional(), weightKg: z.number().min(0).optional(), heartRate: z.number().int().min(20).max(250).optional(), spO2: z.number().min(0).max(100).optional(), temperatureC: z.number().min(20).max(50).optional(), source: z.enum(['manual','photo-device-ocr','ble-sync']).default('manual'), notes: z.string().max(2000).optional() });
const journalSchema = z.object({ timestamp: z.coerce.date().optional(), symptomSeverity: z.number().int().min(0).max(10), primarySymptom: z.string().min(1).max(200), relatedOrganId: z.string().max(100).optional(), contextTags: z.array(z.string().max(100)).max(30).default([]), medicationAdherence: z.enum(['all-taken','partially-taken','skipped']), patientNote: z.string().max(5000).default('') });

router.get('/profile', async (req, res, next) => { try { res.json(await prisma.patientProfile.findUnique({ where: { userId: uid(req) } })); } catch (e) { next(e); } });
router.put('/profile', async (req, res, next) => { try { const data = profileSchema.parse(req.body); const id = uid(req); await ensureUser(id); res.json(await prisma.patientProfile.upsert({ where: { userId: id }, update: data, create: { userId: id, ...data } })); } catch (e) { next(e); } });
router.get('/vitals', async (req, res, next) => { try { res.json(await prisma.vitalEntry.findMany({ where: { userId: uid(req) }, orderBy: { timestamp: 'desc' }, take: 500 })); } catch (e) { next(e); } });
router.post('/vitals', async (req, res, next) => { try { const data = vitalSchema.parse(req.body); const id = uid(req); await ensureUser(id); res.status(201).json(await prisma.vitalEntry.create({ data: { ...data, userId: id, timestamp: data.timestamp ?? new Date() } })); } catch (e) { next(e); } });
router.get('/journal', async (req, res, next) => { try { res.json(await prisma.journalEntry.findMany({ where: { userId: uid(req) }, orderBy: { timestamp: 'desc' }, take: 500 })); } catch (e) { next(e); } });
router.post('/journal', async (req, res, next) => { try { const data = journalSchema.parse(req.body); const id = uid(req); await ensureUser(id); res.status(201).json(await prisma.journalEntry.create({ data: { ...data, userId: id, timestamp: data.timestamp ?? new Date() } })); } catch (e) { next(e); } });
router.get('/alerts', async (req, res, next) => { try { res.json(await prisma.notificationAlert.findMany({ where: { userId: uid(req) }, orderBy: { timestamp: 'desc' } })); } catch (e) { next(e); } });
router.patch('/alerts/:id/read', async (req, res, next) => { try { const result = await prisma.notificationAlert.updateMany({ where: { id: req.params.id, userId: uid(req) }, data: { read: true } }); if (!result.count) return res.status(404).json({ error: 'Alert not found' }); res.json({ success: true }); } catch (e) { next(e); } });
router.get('/reports', async (req, res, next) => { try { res.json(await prisma.medicalReport.findMany({ where: { userId: uid(req) }, orderBy: { createdAt: 'desc' } })); } catch (e) { next(e); } });
router.post('/reports', upload.single('file'), async (req, res, next) => { try { if (!req.file) return res.status(400).json({ error: 'A PDF, JPG, or PNG file is required' }); const id = uid(req); await ensureUser(id); res.status(201).json(await prisma.medicalReport.create({ data: { userId: id, title: req.body.title || req.file.originalname, category: req.body.category || 'Other Report', uploadDate: new Date().toISOString().slice(0,10), documentDate: req.body.documentDate || new Date().toISOString().slice(0,10), facility: req.body.facility || 'Unknown', doctorName: req.body.doctorName, fileType: req.file.mimetype === 'application/pdf' ? 'pdf' : 'photo', fileName: req.file.originalname, filePath: req.file.path } })); } catch (e) { next(e); } });
router.get('/timeline', async (req, res, next) => { try { const id = uid(req); const [reports, vitals, journal] = await Promise.all([prisma.medicalReport.findMany({ where: { userId: id } }), prisma.vitalEntry.findMany({ where: { userId: id } }), prisma.journalEntry.findMany({ where: { userId: id } })]); res.json([...reports.map(r => ({ id: `tl-rep-${r.id}`, timestamp: `${r.documentDate}T10:00:00Z`, source: 'report', title: r.title, badge: r.category, details: r })), ...vitals.map(v => ({ id: `tl-vit-${v.id}`, timestamp: v.timestamp, source: 'vital', title: 'Home Vitals Logged', badge: 'Home Vitals', details: v })), ...journal.map(j => ({ id: `tl-jnl-${j.id}`, timestamp: j.timestamp, source: 'journal', title: j.primarySymptom, badge: 'Symptom Journal', details: j }))].sort((a,b) => new Date(b.timestamp).getTime() - new Date(a.timestamp).getTime())); } catch (e) { next(e); } });
router.get('/specialists', (req, res) => { const organ = typeof req.query.organId === 'string' ? req.query.organId : undefined; res.json(organ ? specialists.filter(s => s.matchingOrgans.includes(organ)) : specialists); });
router.post('/chat/query', async (req, res, next) => { try { const question = z.object({ question: z.string().min(1).max(2000) }).parse(req.body).question; const id = uid(req); const [reports, vitals] = await Promise.all([prisma.medicalReport.findMany({ where: { userId: id }, take: 10, orderBy: { createdAt: 'desc' } }), prisma.vitalEntry.findMany({ where: { userId: id }, take: 10, orderBy: { timestamp: 'desc' } })]); res.json({ answer: 'I can only summarize information stored in your HealthAI record. Please discuss medical decisions with a qualified clinician.', groundedSource: `${reports.length} reports and ${vitals.length} vital entries available`, question, safetyNotice: 'This assistant does not diagnose, prescribe, or replace a clinician.' }); } catch (e) { next(e); } });
